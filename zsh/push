sed -i '$ d' ../README.md && echo 'Modified at:' \``date "+%Y/%m/%d %T"`\` >> ../README.md
git add .
git commit -m $1 -m "" -m "update 🦄 at `date '+%Y-%m-%d %H:%M:%S' $1 ` ⌛️ in ` uname -m` $2 💻"
git push 
